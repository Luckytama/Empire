package de.htwg.se.empire.controller

import org.junit.runner.RunWith
import org.scalatest.{ WordSpec, Matchers }
import org.scalatest.junit.JUnitRunner

@RunWith(classOf[JUnitRunner])
class ReinforcementControllerTest extends WordSpec with Matchers {
  "The reinforcement controller" when {
    val rfController = new ReinforcementController
    "calc soldiers to distribute for player" in {

    }
  }
}
